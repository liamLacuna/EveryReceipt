import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  name: {
    fontSize: 20,
    fontWeight: "500",
    marginTop: 10,
    opacity: 0.5
  },
  base: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center"
  },
  profileText: {
    color: "#0f2962",
    fontSize: 20,
    opacity: 0.5
  }
});

